New clients probability
==============================

## Criando o ambiente para utilização do projeto

- Execute o seguinte comando ```conda create --name new-clients-probability```

- Ative o ambiente ```conda activate new-clients-probability```

- Instale as libs necessárias que estão em requirements.txt ```pip install -r requirements.txt```

- Abra o notebook que se encontra na pasta src com o comando ```jupyter lab```